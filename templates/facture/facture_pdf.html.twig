<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture #{{ commande.idCommande }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .facture-info {
            margin-bottom: 20px;
        }
        .facture-details {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .facture-details th, .facture-details td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        .facture-details th {
            background-color: #f8f9fa;
        }
        .total {
            text-align: right;
            margin-top: 20px;
        }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>FACTURE</h1>
        <p>Numéro de facture: {{ commande.idCommande }}</p>
        <p>Date: {{ date|date('d/m/Y') }}</p>
    </div>

    <div class="facture-info">
        <h3>Détails de la commande</h3>
        <p><strong>Statut:</strong> {{ commande.statueCommande }}</p>
        <p><strong>Date de commande:</strong> {{ commande.dateCommande|date('d/m/Y H:i') }}</p>
        <p><strong>Mode de paiement:</strong> {{ commande.modepaiementCommande }}</p>
        {% if commande.livreurCommande %}
            <p><strong>Livreur:</strong> {{ commande.livreurCommande.nomLivreur }} {{ commande.livreurCommande.prenomLivreur }}</p>
            <p><strong>Contact livreur:</strong> {{ commande.livreurCommande.numeroLivreur }}</p>
        {% endif %}
    </div>

    <table class="facture-details">
        <thead>
            <tr>
                <th>Description</th>
                <th>Montant</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Total de la commande</td>
                <td>{{ commande.prixtotalCommande }} €</td>
            </tr>
        </tbody>
    </table>

    <div class="total">
        <h3>Total à payer: {{ commande.prixtotalCommande }} €</h3>
    </div>

    <div class="footer">
        <p>Merci de votre confiance !</p>
        <p>Pour toute question concernant cette facture, veuillez nous contacter.</p>
    </div>
</body>
</html>
